{
  "name": "ibmcloud-nextjs-custom-server-express",
  "version": "3.0.0",
  "description": "Deploy Next.js server-side React apps using a custom Express server to IBM Cloud",
  "scripts": {
    "dev": "node server.js -p $PORT",
    "build": "next build",
    "start": "NODE_ENV=dev node server.js -p $PORT"
  },
  "dependencies": {
    "express": "^4.16.4",
    "next": "^9.3.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "engines": {
    "node": "10.x"
  }
}
